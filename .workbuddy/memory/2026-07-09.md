# FBS 工作日志 — 2026-07-09

## 上午/中午
- 从 `20260705备份/MSRR/fbs` 恢复 BackScreenController.kt、BackScreenNotificationActivity.kt、MainActivity.kt、PermissionHelper.kt
- 注册 BackScreenNotificationActivity 到 AndroidManifest
- 修复双击手势可用（isClickable=false → 穿透到系统 TouchInteractionService）
- 添加 dispatchKeyEvent(KEYCODE_BACK) 捕获返回手势（系统 SubScreenGestureBack 生成）
- 修复 getOurTaskId() 用 dumpsys activity activities 解析 taskId（Regex: t(\\d+)）
- 改用 context.startActivity() 替代 Shizuku am start（Shizuku 启动的 Activity 无 taskId）
- 添加光线传感器驱动的 AOD 控制（lux<5 持续8秒进AOD，持续5分钟息屏）
- 实现摄像头避开（读取 display.cutout.safeInsetLeft，内容右移）
- 传递完整 styleExtras 到背屏 Activity（字号/颜色/背景/摄像头避开等）
- 首页按钮精简：保留转发、图片贴背屏、样式、清除、粘贴、解析
- 修复超级岛 (SuperIslandHelper) — 加回缺失的 MainActivity handler
- 实现通知清除同步 — FBSNotificationListener → EventChannel → Flutter → dismissBackScreen()
- 修复 SuperIslandHelper 中 ic_menu_home 不存在的编译错误
